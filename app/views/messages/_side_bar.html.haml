.side-bar
  .side-bar__side-top
    .side-bar__box
      .side-bar__masa
        = current_user.name
      .side-bar__aikon
        = link_to new_group_path do
          = icon('fas', 'edit', class: 'icon')
      
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog', class: 'icon')
 
  .side-bar__side-content
    - current_user.groups.each do |group|
      .side-bar__side-bigbox
        = link_to group_messages_path(group) do
          .side-bar__techcamp
            = group.name
          .side-bar__message
            = group.show_last_message
  
  -# .groups
  -#   - current_user.groups.each do |group|
  -#     .group
  -#       = link_to group_messages_path(group) do
  -#         .group__name
  -#           = group.name
  -#         .group__message
  -#           メッセージはまだありません